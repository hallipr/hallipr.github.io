{
  "name": "ark-map-viewer",
  "version": "1.0.0",
  "description": "ARK Map Resource Viewer",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=./root/index.js --format=iife --target=es2020"
  },
  "dependencies": {
    "three": "^0.128.0"
  },
  "devDependencies": {
    "@types/three": "^0.128.0",
    "esbuild": "^0.27.2",
    "typescript": "^5.7.2"
  }
}
